Test fixtures live here.

- `images/` is intended for sample OCR inputs used by regression tests.
- `expected/` is intended for stable expected outputs or reference payloads.

The lightweight generator in `scripts/generate_sample_fixture.py` can create a
sample image fixture for local experiments.
